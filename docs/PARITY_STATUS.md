# Tauri rewrite parity status

Last updated: 2026-09-03

The Python launcher remains the behavior reference. This file records the new Rust/Tauri implementation only.

| Area | Status | Evidence / next gate |
|---|---|---|
| Tauri 2 desktop shell | Verified local foundation | Separate release builds completed. Both portable editions created responsive Win32 webview windows with non-zero handles and the expected `Mythic Loot Launcher Player` / `Mythic Loot Launcher Developer` titles under isolated data roots. Installer execution remains a user-session acceptance gate. |
| React/TypeScript interface | Verified local foundation | Responsive dashboard, Settings, Publisher, Update & Repair and Safe Launch routes visually checked at 1180x760 and the 940x640 minimum. Smart Launch has interaction coverage and a passing production build; its installed-app visual acceptance remains pending. The browser-preview module and all non-native success fallbacks were removed; missing Tauri now fails closed. |
| Rust configuration store | Verified local | Schema 2 contains server-free modpack profiles. Schema 1 migrates atomically while preserving user paths and converts the two old default names. Tests cover defaults, migration, round-trip persistence and corrupt-file preservation. |
| Built-in and remote catalogue | Verified local and live GitHub read | All twelve MLLP game IDs remain available. A strict checked-in v1 public catalogue asynchronously refreshes from the launcher's public `main` branch, adds or updates public modpack identity, version, artwork, manifest and deployment metadata, and preserves every player-local path, launcher choice, launch argument and installed version. Bounded HTTPS reads, duplicate/unsafe field rejection, atomic cache replacement and offline fallback are covered locally. The packaged Player fetched the published two-profile catalogue, cached an exact SHA-256 match and retained blank local machine paths. Full adapter behavior is not ported. |
| Minecraft launcher sync | Verified local foundation / launcher acceptance pending | Existing CurseForge and Modrinth instances are explicit sync targets. Detection includes the current `com.modrinth.theseus` and legacy Modrinth profile roots plus common CurseForge roots; selecting a target persists the launcher kind and routes trusted-manifest Update & Repair into that instance while leaving untracked saves, logs, screenshots, options and launcher/account metadata untouched. Settings generates deterministic minimal CurseForge import ZIP and Modrinth `.mrpack` bootstrap artifacts containing Minecraft 1.21.1/NeoForge 21.1.248 identity with empty file inventories and no machine paths. Actual third-party launcher import and first sync remain external acceptance gates. |
| 7DTD/Steam detection | Verified local foundation | Default and VDF-listed Steam libraries are scanned. Selecting a detected game keeps the Steam root as `gameDir` and derives its `Mods` child as the managed installation/publishing root. Pure path and Settings interaction regressions pass. Real clean-machine detection remains pending. |
| Trusted manifest contract | Verified local and live GitHub read | The actual Minecraft v1.0.1 inventory (2,067 required files and two obsolete paths) and 7DTD v1.0.0 manifest are compile-time bundled. Dedicated HTTPS manifest URLs now refresh asynchronously into launcher-owned cache with a 64 MiB bound; profile/game identity and the complete v1 safety contract are validated before atomic activation, while invalid or unavailable responses preserve the previous cache or bundled fallback. Version 1 rejects unsafe paths, Windows aliases/ADS, invalid hashes, case collisions, obsolete overlap and unsupported URLs. The packaged Player fetched and activated both current GitHub manifests, then a deliberate all-endpoints-offline restart preserved both cached files byte-for-byte while keeping the window responsive. |
| Required-file verification | Verified local | Rust streams SHA-256 comparisons after safe path resolution and reports current, missing, changed and unsafe entries without modifying the install. Dashboard action is enabled after a pack directory is configured. |
| Readiness | Foundation | Game executable, pack directory, trusted manifest and manifest-sourced version gates are live. No network/server state participates. A completed file check can surface Repair Needed in the current session, and Smart Launch consumes the native full-file result before opening. Runtime/Java selection remains. |
| Native game launch | Foundation | Narrow Rust command, validated executable and user-supplied launch arguments exist. Safe Launch retains the exact spawned child for process-exit restoration. The launcher generates no connection or direct-join arguments. Real game acceptance remains blocked by local configuration. |
| Settings/profile editing | Verified local | Developer has a real Add Modpack workspace for normalized, collision-checked profile IDs, built-in game selection, version identity, source folder, artwork, deployment rules and optional GitHub owner/repository wiring. New profiles persist through Rust, become selected immediately and start as catalogue-hidden drafts. A Developer-only visibility control archives or restores released packs in the next public catalogue. Public metadata and distribution controls compile out of Player Settings; Player retains only local paths, launcher target and launch options. |
| Smart Launch | Verified local orchestration / real-game acceptance pending | One action performs full tracked-file verification, refuses unsafe entries, selects version update or changed-file repair, stages through the existing native transaction engine, requires explicit mutation confirmation, refreshes launcher state, rechecks every required file, and opens the configured game or Minecraft launcher only on success. Unit interactions cover clean launch, confirmed update and unsafe-path refusal. Runtime/Java selection and a real configured game lifecycle remain; no server behavior is present. |
| Transactional update/repair | Verified local foundation / external acceptance pending | Rust supports trusted local or HTTPS packages, retries with partial files, multipart assembly, package/member integrity checks, isolated changed-file staging, disk-space gates, cached mutation previews, explicit confirmation, pre-change backup, obsolete removal, complete live post-verification and rollback. Seven updater tests cover update, changed-file-only repair, unsafe archive rejection, confirmation, staging isolation and two rollback paths. A real production GitHub package has not yet been applied. |
| Backups/restore | Verified local foundation | Update/repair backups carry hashed relative-path metadata and update-created removal journals. Recovery history lists only launcher-owned profile backups, retains the newest five, marks legacy/invalid archives unsafe, and requires separate preview plus confirmation for restore or deletion. Restore is isolated and SHA-256 verified, creates a second recovery backup, persists the recorded local version only after live verification, and rolls itself back on failure. Controlled tests pass; a real large installation restore remains an external acceptance gate. |
| Safe Launch | Verified local foundation / real-game acceptance pending | Rust moves only trusted-manifest `optionalFiles`, journals exact paths, sizes and SHA-256 values outside the live install, records the spawned PID, waits for that child to exit, then hash-verifies restoration. Restart recovery refuses a live PID, is bound to the configured installation, requires explicit confirmation and leaves missing, changed or conflicting files untouched. Seven controlled tests cover status, confirmation, launch-failure rollback, PID probing, restoration and journal redirect rejection. The bundled manifests currently declare no optional files, so an actual game lifecycle remains unproven. No server behavior is present. |
| Server handling | Explicitly out of scope | The earlier protocol experiment was removed following owner clarification. There are no server commands, fields, readiness gates, dashboard controls or generated join arguments. |
| News, Rules and Changelog | Verified local editing, rendering and publication foundation / authenticated acceptance pending | Dashboard tabs consume the trusted manifest's typed announcement, HTTPS banner, version/date changelog entries, added/changed/fixed groups, how-to guide, numbered rules and common fixes. Empty manifests show explicit no-content states rather than samples. Developer has a real editor for the same fields, including multiple changelog entries and line-based lists. Rust changes only presentation fields, preserves package URLs/hashes/inventories/version data, enforces bounded content and HTTPS artwork, validates the complete manifest and atomically saves under launcher-owned data. A profile-bound draft manifest can be created before its first package release; an existing malformed manifest is never overwritten. Content-only preparation derives the exact repository/asset from the fixed profile manifest URL, refuses drafts or stale versions, preserves only HTTPS immutable package references, stages one reviewed manifest, re-hashes it at action time and requires authentication plus explicit confirmation before creating a latest manifest-only release. No package ZIP is rebuilt or uploaded. A real authenticated content publication remains an external acceptance gate. |
| Activity Centre | Verified local and packaged smoke | Player and Developer expose the newest 12 real native catalogue, storage, support, setup, verification, update/repair, restore, launch and Developer publishing operations with running, succeeded or failed state and native messages. History persists atomically outside React, retains at most 100 records, preserves malformed files for diagnosis and marks work left running by an earlier launcher process as interrupted. Clear Finished removes only completed work. No sample activity is generated and the empty state is explicit. |
| Storage report/cleanup | Verified local foundation | Player and Developer measure the edition-specific launcher data root plus every configured modpack folder with byte/file/folder totals. Linked, redirected, inaccessible and million-entry-overflow paths are skipped and reported. Configured game/modpack folders are read-only and never cleanup targets. Three typed native actions retain five ZIP restore points per known profile, clear only the fixed catalogue cache, or delete fixed staging/Developer-preview entries older than 24 hours, now including app-update staging and Developer app-release previews. React supplies no path; cleanup is blocked during other native activity and requires a reviewed category plus explicit confirmation. Controlled tests verify fixed-root isolation, recent-temp preservation and backup retention. No real user data was deleted during acceptance. |
| Privacy-redacted support bundle | Verified local foundation / real-log acceptance pending | Player and Developer discover only known Minecraft and 7DTD log locations, refuse links/reparse points and logs over 64 MiB, and retain at most the newest 500 lines / 512 KiB. Rust redacts environment-derived account paths and usernames, obvious credential/token assignments, bearer/GitHub tokens, URL credentials, email, IPv4/IPv6 and hardware addresses. Support shows the exact redacted summary and excerpt before export; the native command accepts only the cached preview ID plus explicit confirmation and atomically writes a SHA-256-reported ZIP under the edition's launcher-owned `support-bundles` folder. The archive is limited to `summary.json`, `summary.txt` and the reviewed excerpt; server configuration, options and full configs are explicitly excluded. Controlled tests cover redaction, known-location discovery, exact ZIP contents and fail-closed confirmation. No real personal game log was exported during acceptance. |
| GitHub repository creation | Foundation / external acceptance pending | Developer UI and Rust commands perform read-only `gh` preflight, validate strict `owner/name` input, default to private, preview the external mutation, and fail closed without explicit confirmation. No repository was created during local verification and no token is stored. |
| GitHub release packaging/upload | Verified local foundation / external acceptance pending | Rust walks a selected source without following links, excludes runtime/private state and non-runtime upstream documentation, scans supported text through 32 MiB, rejects personal/credential-shaped runtime content, streams SHA-256, produces sorted fixed-metadata ZIPs and validated v1 manifests, and previews exact assets. ZIPs at or above 2 GiB split into ordered 1 GiB assets; the manifest hashes each part and the complete ZIP, the updater verifies both levels, and cached publication re-hashes every reviewed asset. A successful upload atomically records the new version/latest-manifest URL, activates the exact reviewed manifest locally for subsequent content editing and enables catalogue visibility. The real 2.727 GiB 7DTD source completed local multipart acceptance. Tests never create a GitHub release; authenticated real-repository publication remains unproven. |
| Player catalogue publication | Verified local foundation / authenticated acceptance pending | Developer projects visible profiles into the public-only v1 contract, previews every included profile plus hidden-draft count, writes a native cached JSON artifact, re-parses and re-hashes it at action time, and requires a separate confirmation. Publication targets only `HixxyDubz/Mythic-Loot-Launcher-Rust-`, branch `main`, file `launcher-catalog.json` through authenticated `gh api`; React cannot supply another repository, branch, path or token. Pure generation, privacy exclusion, invalid-profile blocking and fail-closed confirmation tests pass. The checked-in initial catalogue is valid; the in-app GitHub replacement action has not been invoked. |
| Player self-update and Developer app publication | Verified live end to end | Player checks the fixed `launcher-update-player.json` feed on `HixxyDubz/Mythic-Loot-Launcher-Rust-` with a per-check cache nonce so GitHub's latest-release redirect is freshly resolved. It validates a strict Player-only schema, semantic versions, fixed HTTPS asset identity, size and SHA-256, downloads into launcher-owned staging and requires explicit review before starting an external helper. The Player and its copied helper explicitly declare Windows `asInvoker`; the helper uses a neutral filename, waits for Player to exit, compares canonical directories across normal and `\\?\\` path forms, re-verifies current and staged executables, retains an exact backup, activates through adjacent atomic renames, verifies the installed hash, rolls back and verifies restoration on failure, writes a result journal and restarts Player. Developer's app-release workspace accepts a BOM-free real build manifest, independently re-hashes the Player portable and installer, stages exactly those two assets plus the checksum feed, revalidates the cached plan and requires separate confirmation before a version-tagged `gh release create`; repository, filenames and feed destination are fixed in Rust, and no Developer binary or token can enter the release. The visible packaged Developer workflow published the exact three-asset 0.1.7 latest release. The exact published 0.1.6 Player then reviewed and downloaded that live feed, verified and activated the published 0.1.7 executable, retained an exact 0.1.6 backup, wrote a successful result journal and restarted into a responsive Player window. Repository-level immutable releases remain a separate GitHub setting and are not assumed. |
| Player/Developer packaging and privacy audit | Verified local separation / installer acceptance pending | Player and Developer use distinct product names, Windows identifiers, installers, portable EXEs and data directories. Player's Vite build resolves an edition module that never imports Publisher, Developer Modpack Manager, content editor/publisher, app-release publisher or public-metadata editing. Its Rust build disables the `developer` feature so all Developer publisher modules and commands are absent. Packaging rejects Developer workspace labels, distribution fields, content/app-release publication text and IPC identifiers in Player output as well as former preview-data symbols in both editions. Both current portable EXEs passed the repeatable isolated responsive-window/config/native-activity smoke script. The NSIS installers were built but not installed. |

## Verification snapshot

- `cargo test --manifest-path src-tauri/Cargo.toml`: 93 passed and one explicit live-source acceptance harness ignored by default, including latest-feed revalidation, canonical helper-journal path validation, app-feed/release validation, executable replacement and rollback in addition to privacy redaction, support isolation, storage cleanup, activity recovery, catalogue validation, setup formats, schema recovery, deterministic publishing, update/restore rollback and Safe Launch guards.
- `npm run test`: 25 passed across 12 files, including exact app-update checksum review, confirmation-gated restart, absent-feed truthfulness and fixed three-asset Developer app publication as well as the existing support, storage, Activity, content, catalogue, modpack, Player-separation and Smart Launch flows.
- Player frontend build passed with 1,836 modules transformed and zero Publisher/Modpack Manager UI, public-metadata fields, content/app-publication IPC or former preview-data identifiers in its output; Developer passed with 1,842 modules and the expected Developer workspace identifiers.
- Player's exact `--no-default-features` native suite passed 68 tests. The full Developer/default suite passed 93 tests with one explicit live-source harness ignored.
- Clippy passed with warnings denied for both `--all-features` Developer and `--no-default-features` Player targets.
- `npm run package:windows`: two NSIS installers and two portable win-unpacked executables built into edition-specific folders with one SHA-256 manifest. `npm run smoke:windows` then opened both portable builds under unique isolated data roots, verified exact edition titles, non-zero window handles, real configuration creation and native `activity-history.json` creation, closed them and removed only the checked smoke directory.
- `npm run smoke:app-update`: the packaged Player helper replaced a temporary Player executable with deliberately different reviewed bytes, preserved the exact original backup, verified the activated SHA-256, wrote a successful result journal, executed the replaced binary's restart probe and removed its isolated fixture.
- Live app-release acceptance: the packaged Developer app visibly loaded the real BOM-free Windows build manifest, displayed both exact Player hashes, required confirmation, and published the fixed executable, installer and checksum feed as GitHub's latest 0.1.7 release.
- `npm run smoke:live-app-update -- -FromVersion 0.1.6 -ExpectedVersion 0.1.7`: the preserved exact published 0.1.6 Player fetched the newly published feed without waiting for a stale latest redirect, displayed and downloaded the reviewed 0.1.7 SHA-256, started its non-elevated helper, preserved an exact backup, activated the exact published bytes, wrote success and restarted into a responsive Player window.
- Browser visual check: the Update & Repair, Recovery history and Safe Launch workspaces passed at 1180x760 and 940x640 without horizontal overflow. Safe Launch truthfully showed that the bundled manifest has no optional extras, omitted the unavailable Start action, remained readable and logged no browser warnings or errors.
- Player 0.1.7 handoff: installer is 5,235,415 bytes with SHA-256 `D74BACE4D9BE7346D529192A2E44ED29A67AE225F058CD245EEAAC56597B9D5A`; portable EXE is 17,587,712 bytes with SHA-256 `146CB797A6A352DBC3C94B99CA9D2B2DE64108FA7A5C2AEC72225A535A2AE8E1`.
- Developer 0.1.7 handoff: installer is 5,402,438 bytes with SHA-256 `B02A445B66E19C37DB08655111B8EA6ED90686CA46934BEE782764B051F03649`; portable EXE is 18,426,880 bytes with SHA-256 `1A9DC0E77BB6821D3928BC9B51F57CBEE41AE753DE10717B70B6D34F27A4004A`.
- Both current portable editions created responsive windows with edition-correct titles and isolated schema-2 state containing two profiles. The Player's packaged networking fetched the 616,266-byte Minecraft v1.0.1 manifest with 2,067 tracked files and the 654-byte 7DTD v1.0.0 manifest from GitHub. With the catalogue and both manifest URLs then redirected to deliberately unreachable local HTTPS endpoints, the Player remained responsive and retained SHA-256 `15AE1D3FEE15A042114C3B1A7419AD9A8AE5E36BC3E93A3D95759F5A8600EEBD` and `EC00CA259B7DA87DD866F902C6CECAEB77396D5689F3B200618C6AC3FCA4139E` respectively. Temporary smoke data was removed afterward.
- After commit `f605f10` reached `origin/main`, the current packaged Player fetched the live public `launcher-catalog.json`, stayed responsive, loaded catalogue version 1.0 with two profiles, created schema-2 configuration with no machine-local paths, and cached SHA-256 `C28FD8F0EB440608724030677738162975FE29641A004E3F0E2354A492611FD9`, exactly matching the checked-in file. Isolated acceptance data was removed afterward.
- Live 7DTD acceptance was read-only: `C:\Program Files (x86)\Steam\steamapps\common\7 Days To Die\Mods` contains 186 top-level folders and 4,107 files totalling 2,927,899,865 bytes (2.727 GiB), with no reparse points. The first scan correctly stopped on contact/example-path data in two upstream documentation files; the documented exclusion policy then retained runtime files and a complete local multipart build plus manifest validation passed in 538.45 seconds. No GitHub call occurred and disposable output was removed.

## External acceptance boundary

Server acceptance is not a launcher gate because servers are managed elsewhere. Authenticated real-repository content-only publication, authenticated in-app catalogue replacement, a full real-package GitHub update/repair, real large-installation restore, authenticated GitHub repository creation/release publication of the locally verified multipart assets, real import of the generated CurseForge and Modrinth bootstrap profiles, installation of both NSIS editions, a real configured-game Smart Launch, a real-game Safe Launch using a trusted manifest with optional files, and friend-machine modpack acceptance are not part of this checkpoint.
