import type { EditionPublisherProps } from "./types";

export const launcherEdition = "player" as const;
export const publisherAvailable = false;

export function EditionPublisherPanel(_props: EditionPublisherProps) {
  return null;
}
